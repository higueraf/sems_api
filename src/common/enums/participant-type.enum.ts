/** Rol de participación del autor — determina requisitos de postulación (ver ScientificProductType). */
export enum ParticipantType {
  PROFESSOR = 'profesor',
  STUDENT   = 'estudiante',
  GRADUATE  = 'profesional_graduado',
}
